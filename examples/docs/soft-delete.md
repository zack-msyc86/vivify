# ソフトデリートの方針

タスクやプロジェクトの削除はすべてソフトデリートで行う。

## 背景

タスク管理ツールでは「うっかり削除」が致命的になりやすい。物理削除にすると復元手段がバックアップしかなくなり、運用負荷が高い。ソフトデリートなら即座に復元でき、ユーザー問い合わせにも迅速に対応できる。

## ルール

- `deleted_at` カラムで管理し、`NULL` なら有効なレコードとする
- 一覧取得のクエリにはデフォルトで `deleted_at IS NULL` を含める
- 完全削除（パージ）は管理者のみが実行でき、削除から 30 日経過後に限る
