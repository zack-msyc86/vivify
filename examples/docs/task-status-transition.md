# タスクのステータス遷移ルール

タスクのステータス変更には決められた遷移パスのみを許可する。

## 背景

自由にステータスを変更できると「完了→未着手」のような意味のない遷移が発生し、進捗の追跡やレポートが不正確になる。遷移を制限することで、ワークフローの整合性を保つ。

## 許可する遷移

- `todo` → `in_progress`
- `in_progress` → `done` / `todo`（差し戻し）
- `done` → `in_progress`（再オープン）

これ以外の遷移はドメイン層でバリデーションエラーにする。
